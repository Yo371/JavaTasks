package cleancode.planes;

public class MoreFunctionalPlane extends Plane {
    public MoreFunctionalPlane(String model, int maxSpeed, int maxFlightDistance, int maxLoadCapacity) {
        super(model, maxSpeed, maxFlightDistance, maxLoadCapacity);
    }
}
